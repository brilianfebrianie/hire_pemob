import 'package:flutter/material.dart';

class HelpInHistoryPage extends StatelessWidget {
  const HelpInHistoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
